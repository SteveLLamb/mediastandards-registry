name: Validate Document URLs

on:
  schedule:
    - cron: "15 04 1,15 * *"   # 21:15 PT prev-day, 1st + 15th of each month (cron in UTC).
                               # Cadence dropped from weekly to biweekly when the
                               # corpus passed ~26k docs — each sweep takes hours,
                               # publishers throttle the request rate, and standards
                               # URLs rarely drift inside a 14-day window. See
                               # CHANGELOG.md.
  workflow_run:
    workflows: ["Build MasterReference Index"]
    types: [completed]
  workflow_dispatch:

permissions:
  actions: read
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: validate-url
  cancel-in-progress: true

jobs:
  validate-url:
    if: 
      github.event_name != 'workflow_run' || 
      github.event.workflow_run.conclusion == 'success'
    name: Validate URLs
    runs-on: ubuntu-latest

    steps:
      - name: Check upstream PR marker
        id: guard
        if: github.event_name == 'workflow_run'
        uses: actions/github-script@v9
        with:
          script: |
            const run = context.payload.workflow_run;
            if (!run) { core.setOutput('skip', 'false'); return; }
            const { owner, repo } = context.repo;
            const artifacts = await github.paginate(github.rest.actions.listWorkflowRunArtifacts, {
              owner, repo, run_id: run.id, per_page: 100
            });
            const marker = artifacts.find(a => a.name && a.name.startsWith('skip-site-build-pr-'));
            const prMatch = marker && /^skip-site-build-pr-(\d+)$/.test(marker.name) ? marker.name.match(/^skip-site-build-pr-(\d+)$/)[1] : '';
            const prNum = prMatch || '';
            core.info(`skip-site-build artifact present: ${!!marker}${prNum ? ` (PR #${prNum})` : ''}`);

            if (marker) {
              core.setOutput('skip', 'true');
              core.setOutput('pr', prNum);
              core.setOutput('skip_reason', 'upstream-pr-open');
              core.setOutput('last_run_url', '');
              return;
            }

            // Biweekly throttle: for workflow_run-triggered executions, skip if a
            // successful Validate URL run already completed within the last 14 days.
            // Cron fires on the 1st + 15th; this throttle prevents the upstream
            // `Build MasterReference Index` workflow_run trigger from firing extra
            // sweeps in between — full URL revalidation against the now-26k-doc
            // corpus is too expensive (and too rate-limit-friendly with upstream
            // publishers) to repeat more than ~biweekly.
            const now = Date.now();
            const throttleWindowMs = 14 * 24 * 60 * 60 * 1000;
            const recentRuns = await github.paginate(github.rest.actions.listWorkflowRunsForRepo, {
              owner,
              repo,
              status: 'completed',
              per_page: 100
            });

            const candidates = recentRuns
              .filter(r =>
              r.id !== context.runId &&
              r.name === context.workflow &&
              r.conclusion === 'success' &&
              (now - new Date(r.created_at).getTime()) < throttleWindowMs
              )
              .sort((a, b) => new Date(b.created_at).getTime() - new Date(a.created_at).getTime());

            async function ranValidationStepSuccessfully(runId) {
              const jobs = await github.paginate(github.rest.actions.listJobsForWorkflowRun, {
                owner,
                repo,
                run_id: runId,
                per_page: 100
              });
              for (const job of jobs) {
                const steps = Array.isArray(job?.steps) ? job.steps : [];
                const validateStep = steps.find((s) => s?.name === 'Run URL validation');
                if (validateStep && validateStep.conclusion === 'success') return true;
              }
              return false;
            }

            let recentSuccess = null;
            for (const run of candidates) {
              if (await ranValidationStepSuccessfully(run.id)) {
                recentSuccess = run;
                break;
              }
            }

            if (recentSuccess) {
              core.info(`Biweekly throttle hit. Recent successful URL-validate run: ${recentSuccess.html_url}`);
              core.setOutput('skip', 'true');
              core.setOutput('pr', '');
              core.setOutput('skip_reason', 'biweekly-throttle');
              core.setOutput('last_run_url', recentSuccess.html_url || '');
              return;
            }

            core.setOutput('skip', 'false');
            core.setOutput('pr', '');
            core.setOutput('skip_reason', '');
            core.setOutput('last_run_url', '');

      - name: Mint PrZ3 Unit token
        if: steps.guard.outputs.skip != 'true'
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Skipping Validate URLs (upstream PR open)
        if: steps.guard.outputs.skip == 'true'
        run: |
          if [ "${{ steps.guard.outputs.skip_reason }}" = "biweekly-throttle" ]; then
            echo "⏭️  Skipping URL validate due to biweekly throttle (already succeeded within last 14 days)."
            echo "Last successful run: ${{ steps.guard.outputs.last_run_url }}"
          else
            echo "⏭️  Skipping URL validate because upstream workflow opened PR #${{ steps.guard.outputs.pr }}."
          fi


      - name: Checkout repo
        if: steps.guard.outputs.skip != 'true'
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}
          # Don't persist the checkout token as an http.extraheader. The job
          # routinely runs 1-2h (URL validation against the ~26k-doc corpus),
          # so by the time the auto-commit pushes, the original token is long
          # expired. We re-mint a fresh token below and inject it via
          # `git remote set-url`, but a persisted extraheader carrying the
          # stale token overrides the URL credential and the push fails with
          # "Invalid username or token". Disabling persistence leaves the
          # fresh-token remote URL as the sole credential. See run 27840389788.
          persist-credentials: false

      - name: Set up Node.js
        if: steps.guard.outputs.skip != 'true'
        uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        if: steps.guard.outputs.skip != 'true'
        run: npm ci

      - name: Run URL validation
        if: steps.guard.outputs.skip != 'true'
        run: npm run validate-url

      - name: Upload validation report (if exists)
        if: (success() || failure()) && steps.guard.outputs.skip != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: url-validation-report
          path: src/main/reports/url_validate_audit.json
          if-no-files-found: ignore

      - name: Read redirectUndefinedCount from audit
        id: audit
        if: (success() || failure()) && steps.guard.outputs.skip != 'true'
        run: |
          if [ ! -f src/main/reports/url_validate_audit.json ]; then
            echo "redirect_undefined=0" >> $GITHUB_OUTPUT
            echo "redirectUndefinedCount=0"
            exit 0
          fi
          RU=$(jq -r '.index.redirectBreakdown.undefined // 0' src/main/reports/url_validate_audit.json)
          echo "redirect_undefined=$RU" >> $GITHUB_OUTPUT
          echo "redirectUndefinedCount=$RU"

      # The App token minted at job start (steps.app-token) is hard-capped at
      # a 1-hour TTL by GitHub. The Run URL validation step routinely takes
      # 90+ minutes against a 26k-doc corpus, so by the time the auto-commit
      # below runs the original token is dead — `git push` falls through to
      # a credential prompt and the job exits 128 ("could not read Username
      # for https://github.com"). Re-mint a fresh token here, then steps
      # downstream of this point reference `steps.app-token-refresh.outputs.token`.
      - name: Refresh App token (validate-url runs past 1h expiry)
        id: app-token-refresh
        if: (success() || failure()) && steps.guard.outputs.skip != 'true'
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Auto-commit audit back to main (if changed)
        if: (success() || failure()) && steps.guard.outputs.skip != 'true'
        env:
          GITHUB_TOKEN: ${{ steps.app-token-refresh.outputs.token }}
        run: |
          set -e
          BR="${{ github.event.repository.default_branch }}"
          git config user.name "PrZ3 Unit"
          git config user.email "prz3-unit[bot]@users.noreply.github.com"
          # The token stored in .git/config by actions/checkout is the
          # original (now-expired) App token. Inject the fresh token into
          # the remote URL so `git push` actually authenticates with it.
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
          git add src/main/reports/url_validate_audit.json || true
          if git diff --cached --quiet; then
            echo "No audit changes to commit.";
            exit 0;
          fi
          git commit -m "chore: url validation audit update"
          git push origin HEAD:"$BR"
          echo "✅ Committed url_validate_audit.json to ${BR}"

      - name: Sync to latest base after audit push
        if: steps.guard.outputs.skip != 'true'
        env:
          GITHUB_TOKEN: ${{ steps.app-token-refresh.outputs.token }}
        run: |
          git fetch origin
          git checkout ${{ github.event.repository.default_branch }}
          git reset --hard origin/${{ github.event.repository.default_branch }}

      - name: Run URL normalizer (apply) for missing resolved*
        if: steps.guard.outputs.skip != 'true' && steps.audit.outputs.redirect_undefined != '0'
        run: |
          node src/main/scripts/url.normalize.js --apply

      - name: Read normalize applied count
        id: norm
        if: steps.guard.outputs.skip != 'true' && steps.audit.outputs.redirect_undefined != '0'
        run: |
          if [ -f src/main/reports/url_validate_normalize.json ]; then
            APPLIED=$(jq -r '.applied // 0' src/main/reports/url_validate_normalize.json)
          else
            APPLIED=0
          fi
          echo "applied=$APPLIED" >> $GITHUB_OUTPUT
          echo "normalizeApplied=$APPLIED"

      - name: Validate document registry
        if: steps.guard.outputs.skip != 'true' && steps.norm.outputs.applied != '0'
        run: npm run validate

      - name: Canonicalize document registry
        if: steps.guard.outputs.skip != 'true' && steps.norm.outputs.applied != '0'
        env:
          IS_PR_RUN: true
          PR_LOG_PATH: ${{ runner.temp }}/pr-log.log
        run: npm run canonicalize

      - name: Commit canonicalized document registry
        if: steps.guard.outputs.skip != 'true' && steps.norm.outputs.applied != '0'
        run: |
          git config user.name "PrZ3 Unit"
          git config user.email "prz3-unit[bot]@users.noreply.github.com"
          git add -A src/main/data/docs/
          if git diff --cached --quiet; then
            echo "No canonicalized changes to commit."
            exit 0
          fi
          git commit -m "Canonicalize document registry after normalize"

      - name: Create PR for normalized URLs
        if: steps.guard.outputs.skip != 'true' && steps.norm.outputs.applied != '0' && github.ref != 'refs/heads/chore/url-normalize'
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ steps.app-token-refresh.outputs.token }}
          commit-message: 'fix: backfill resolved* from URL validation (missing resolved)'
          title: "URL Backfill resolved* for ${{ steps.audit.outputs.redirect_undefined }} entries"
          body: |
            This PR backfills resolved* fields for entries missing them based on the URL validation audit.
            - Source: src/main/reports/url_validate_audit.json
            - Script: src/main/scripts/url.normalize.js --apply
            - Detected missing resolved*: ${{ steps.audit.outputs.redirect_undefined }}
            - Applied backfills: ${{ steps.norm.outputs.applied }}
          labels: automated, reports, url
          branch: chore/url-normalize
          base: ${{ github.event.repository.default_branch }}
          delete-branch: true
          add-paths: |
            src/main/data/docs
            src/main/reports/url_validate_normalize.json

      - name: Upsert URL issues
        # Uses the workflow's GITHUB_TOKEN (which already has issues:write per
        # the top-level `permissions:` block) rather than the App token. The
        # App token mint above is kept for steps that need bot-identity
        # commits, but issue management against `actions/github-script` only
        # needs `issues: write` — GITHUB_TOKEN's scope is sufficient and
        # avoids the 401 path when the App's installation permissions drift
        # (which is what caused run #27727667119 to fail with "Bad credentials"
        # on issues.listForRepo).
        if: (success() || failure()) && steps.guard.outputs.skip != 'true'
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const path = require('path');

            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const auditPath = path.join(process.cwd(), 'src', 'main', 'reports', 'url_validate_audit.json');

            if (!fs.existsSync(auditPath)) {
              core.info('No audit file found; skipping issue upsert.');
              return;
            }

            const audit = JSON.parse(fs.readFileSync(auditPath, 'utf8'));
            const genAt = audit.generatedAt || new Date().toISOString();
            const runTag = `Run #${context.runId}`;

            // Resolve default branch for a stable link to the audit JSON
            const defaultBranch = (context.payload && context.payload.repository && context.payload.repository.default_branch) || 'main';
            const auditUrl = `https://github.com/${owner}/${repo}/blob/${defaultBranch}/src/main/reports/url_validate_audit.json`;


            const unr = (audit.report && audit.report.unreachable) || {};
            const list404 = Array.isArray(unr['404']) ? unr['404'] : [];
            const list400 = Array.isArray(unr['400']) ? unr['400'] : [];
            const listENF = Array.isArray(unr['ENOTFOUND']) ? unr['ENOTFOUND'] : [];
            const listREDIR = Array.isArray(unr['ERR_FR_TOO_MANY_REDIRECTS']) ? unr['ERR_FR_TOO_MANY_REDIRECTS'] : [];
            const exp = (audit.report && audit.report.expectation) || {};
            const listSMPTEStdPrefix = Array.isArray(exp['SMPTE.resolvedHref.standards-prefix']) ? exp['SMPTE.resolvedHref.standards-prefix'] : [];
            // --- Additional error types ---
            const list403 = Array.isArray(unr['403']) ? unr['403'] : [];
            const listHPE = Array.isArray(unr['HPE_INVALID_CONSTANT']) ? unr['HPE_INVALID_CONSTANT'] : [];
            const listBR  = Array.isArray(unr['ERR_BAD_REQUEST']) ? unr['ERR_BAD_REQUEST'] : [];
            const listLEAF = Array.isArray(unr['UNABLE_TO_VERIFY_LEAF_SIGNATURE']) ? unr['UNABLE_TO_VERIFY_LEAF_SIGNATURE'] : [];

            // Build current state: 404 by publisher, 400 by docId
            const byPub404 = new Map(); // pub -> array of entries
            for (const item of list404) {
              const pub = (item.publisher || 'Unknown').trim();
              if (!byPub404.has(pub)) byPub404.set(pub, []);
              byPub404.get(pub).push(item);
            }
            const current400 = new Set(list400.map(i => i.docId));

            // Helper: list all open issues with all labels (all must match)
            async function listOpenWithLabel(...labels) {
              const labelStr = labels.filter(Boolean).join(',');
              const issues = await github.paginate(github.rest.issues.listForRepo, {
                owner, repo, state: 'open', labels: labelStr, per_page: 100
              });
              // Exclude PRs (listForRepo returns both)
              return issues.filter(i => !i.pull_request);
            }

            // Helper: create or update issue
            async function upsertIssue({title, body, labels}) {
              // Search open issues by exact title
              const openIssues = await github.paginate(github.rest.issues.listForRepo, {
                owner, repo, state: 'open', per_page: 100
              });
              const found = openIssues.find(i => i.title === title && !i.pull_request);
              if (found) {
                await github.rest.issues.update({ owner, repo, issue_number: found.number, body, labels });
                return found.number;
              } else {
                const created = await github.rest.issues.create({ owner, repo, title, body, labels });
                return created.data.number;
              }
            }

            // Upsert 404-by-publisher issues
            for (const [pub, arr] of byPub404.entries()) {
              const title = `URL ERROR (404): ${pub}`;
              const lines = arr
                .sort((a,b) => (a.docId || '').localeCompare(b.docId || ''))
                .slice(0, 100)
                .map(i => `- docId: **${i.docId}**\n_${i.field}_: ${i.url}`);
              const body = [
                '**Recommended action:** Double-check the URL or remove it if no longer valid.',
                '',
                `**Count:** ${arr.length}`,
                (arr.length > 100
                  ? `Shown below: first 100. Remaining ${arr.length - 100} in the [Full audit JSON](${auditUrl})`
                  : `[Full audit JSON](${auditUrl})`),
                '',
                ...lines,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', '404'] });
            }

            // Upsert 403-by-publisher issues
            const byPub403 = new Map(); // pub -> array of entries
            for (const item of list403) {
              const pub = (item.publisher || 'Unknown').trim();
              if (!byPub403.has(pub)) byPub403.set(pub, []);
              byPub403.get(pub).push(item);
            }
            for (const [pub, arr] of byPub403.entries()) {
              const title = `URL ERROR (403): ${pub}`;
              const lines = arr
                .sort((a,b) => (a.docId || '').localeCompare(b.docId || ''))
                .slice(0, 100)
                .map(i => `- docId: **${i.docId}**\n_${i.field}_: ${i.url}`);
              const body = [
                '**Recommended action:** Check if the resource requires authentication or is private.',
                '',
                `**Count:** ${arr.length}`,
                (arr.length > 100
                  ? `Shown below: first 100. Remaining ${arr.length - 100} in the [Full audit JSON](${auditUrl})`
                  : `[Full audit JSON](${auditUrl})`),
                '',
                ...lines,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', '403'] });
            }

            // Upsert ENOTFOUND-by-publisher issues
            const byPubENF = new Map(); // pub -> array of entries
            for (const item of listENF) {
              const pub = (item.publisher || 'Unknown').trim();
              if (!byPubENF.has(pub)) byPubENF.set(pub, []);
              byPubENF.get(pub).push(item);
            }
            for (const [pub, arr] of byPubENF.entries()) {
              const title = `URL ERROR (ENOTFOUND): ${pub}`;
              const lines = arr
                .sort((a,b) => (a.docId || '').localeCompare(b.docId || ''))
                .slice(0, 100)
                .map(i => `- docId: **${i.docId}**\n_${i.field}_: ${i.url}`);
              const body = [
                '**Recommended action:** Check for typos or outdated domain.',
                '',
                `**Count:** ${arr.length}`,
                (arr.length > 100
                  ? `Shown below: first 100. Remaining ${arr.length - 100} in the [Full audit JSON](${auditUrl})`
                  : `[Full audit JSON](${auditUrl})`),
                '',
                ...lines,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', 'enotfound'] });
            }

            // Upsert HPE_INVALID_CONSTANT-by-publisher issues
            const byPubHPE = new Map(); // pub -> array of entries
            for (const item of listHPE) {
              const pub = (item.publisher || 'Unknown').trim();
              if (!byPubHPE.has(pub)) byPubHPE.set(pub, []);
              byPubHPE.get(pub).push(item);
            }
            for (const [pub, arr] of byPubHPE.entries()) {
              const title = `URL ERROR (HPE_INVALID_CONSTANT): ${pub}`;
              const lines = arr
                .sort((a,b) => (a.docId || '').localeCompare(b.docId || ''))
                .slice(0, 100)
                .map(i => `- docId: **${i.docId}**\n_${i.field}_: ${i.url}`);
              const body = [
                '**Recommended action:** Verify the URL manually. It may be non-HTTP content.',
                '',
                `**Count:** ${arr.length}`,
                (arr.length > 100
                  ? `Shown below: first 100. Remaining ${arr.length - 100} in the [Full audit JSON](${auditUrl})`
                  : `[Full audit JSON](${auditUrl})`),
                '',
                ...lines,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', 'hpe-invalid-constant'] });
            }

            // Upsert ERR_FR_TOO_MANY_REDIRECTS-by-publisher issues
            const byPubREDIR = new Map(); // pub -> array of entries
            for (const item of listREDIR) {
              const pub = (item.publisher || 'Unknown').trim();
              if (!byPubREDIR.has(pub)) byPubREDIR.set(pub, []);
              byPubREDIR.get(pub).push(item);
            }
            for (const [pub, arr] of byPubREDIR.entries()) {
              const title = `URL ERROR (ERR_FR_TOO_MANY_REDIRECTS): ${pub}`;
              const lines = arr
                .sort((a,b) => (a.docId || '').localeCompare(b.docId || ''))
                .slice(0, 100)
                .map(i => `- docId: **${i.docId}**\n_${i.field}_: ${i.url}`);
              const body = [
                '**Recommended action:** Check the server config or redirect chain.',
                '',
                `**Count:** ${arr.length}`,
                (arr.length > 100
                  ? `Shown below: first 100. Remaining ${arr.length - 100} in the [Full audit JSON](${auditUrl})`
                  : `[Full audit JSON](${auditUrl})`),
                '',
                ...lines,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', 'too-many-redirects'] });
            }

            // Upsert SMPTE.resolvedHref.standards-prefix expectation mismatches by publisher
            const byPubSMPTEStd = new Map(); // pub -> array of entries
            for (const item of listSMPTEStdPrefix) {
              const pub = (item.publisher || 'Unknown').trim();
              if (!byPubSMPTEStd.has(pub)) byPubSMPTEStd.set(pub, []);
              byPubSMPTEStd.get(pub).push(item);
            }
            for (const [pub, arr] of byPubSMPTEStd.entries()) {
              const title = `EXPECTATION MISMATCH (SMPTE.resolvedHref.standards-prefix): ${pub}`;
              const lines = arr
                .sort((a,b) => (a.docId || '').localeCompare(b.docId || ''))
                .slice(0, 100)
                .map(i => `- docId: **${i.docId}**\n_${i.field}_: ${i.actual}`);
              const body = [
                '**Recommended action:** End point has likely changed, check the url.rules',
                '',
                `**Count:** ${arr.length}`,
                (arr.length > 100
                  ? `Shown below: first 100. Remaining ${arr.length - 100} in the [Full audit JSON](${auditUrl})`
                  : `[Full audit JSON](${auditUrl})`),
                '',
                ...lines,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', 'standards-prefix'] });
            }

            // Upsert 400-per-item issues
            for (const i of list400) {
              const title = `URL ERROR (400): ${i.docId}`;
              const body = [
                '**Recommended action:** Inspect for typos or formatting issues.',
                '',
                `docId: **${i.docId}**`,
                `- _${i.field}_: ${i.url}`,
                `[Full audit JSON](${auditUrl})`,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', '400'] });
            }

            // Upsert ERR_BAD_REQUEST-per-item issues
            for (const i of listBR) {
              const title = `URL ERROR (ERR_BAD_REQUEST): ${i.docId}`;
              const body = [
                '**Recommended action:** Validate the URL format or encoding.',
                '',
                `docId: **${i.docId}**`,
                `- _${i.field}_: ${i.url}`,
                `[Full audit JSON](${auditUrl})`,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', 'bad-request'] });
            }

            // Upsert UNABLE_TO_VERIFY_LEAF_SIGNATURE-per-item issues
            for (const i of listLEAF) {
              const title = `URL ERROR (UNABLE_TO_VERIFY_LEAF_SIGNATURE): ${i.docId}`;
              const body = [
                '**Recommended action:** Avoid or investigate SSL issues before trusting the source.',
                '',
                `docId: **${i.docId}**`,
                `- _${i.field}_: ${i.url}`,
                `[Full audit JSON](${auditUrl})`,
                '',
                `_Created automatically from URL validation audit on ${genAt} — ${runTag}_`
              ].join('\n');
              await upsertIssue({ title, body, labels: ['automated', 'url', 'leaf-signature'] });
            }

            // Autoclose resolved issues (404, 403, ENOTFOUND, HPE_INVALID_CONSTANT,
            //  ERR_FR_TOO_MANY_REDIRECTS, SMPTE.resolvedHref.standards-prefix, 400, ERR_BAD_REQUEST, UNABLE_TO_VERIFY_LEAF_SIGNATURE)
            const open404 = await listOpenWithLabel('url', '404');
            const open403 = await listOpenWithLabel('url', '403');
            const openENF = await listOpenWithLabel('url', 'enotfound');
            const openHPE = await listOpenWithLabel('url', 'hpe-invalid-constant');
            const openREDIR = await listOpenWithLabel('url', 'too-many-redirects');
            const openSMPTEStd = await listOpenWithLabel('url', 'standards-prefix');
            const open400 = await listOpenWithLabel('url', '400');
            const openBR = await listOpenWithLabel('url', 'bad-request');
            const openLEAF = await listOpenWithLabel('url', 'leaf-signature');

            // 404s: close any publisher issue not in current set
            const livePubs = new Set([...byPub404.keys()]);
            for (const iss of open404) {
              const m = /^URL ERROR \(404\): (.+)$/.exec(iss.title || '');
              const pub = m && m[1] ? m[1].trim() : null;
              if (!pub || !livePubs.has(pub)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no outstanding 404s for this publisher. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // 403s: close any publisher issue not in current set
            const livePubs403 = new Set([...byPub403.keys()]);
            for (const iss of open403) {
              const m = /^URL ERROR \(403\): (.+)$/.exec(iss.title || '');
              const pub = m && m[1] ? m[1].trim() : null;
              if (!pub || !livePubs403.has(pub)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no outstanding 403s for this publisher. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // ENOTFOUNDs: close any publisher issue not in current set
            const liveENFPubs = new Set([...byPubENF.keys()]);
            for (const iss of openENF) {
              const m = /^URL ERROR \(ENOTFOUND\): (.+)$/.exec(iss.title || '');
              const pub = m && m[1] ? m[1].trim() : null;
              if (!pub || !liveENFPubs.has(pub)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no outstanding ENOTFOUNDs for this publisher. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // HPE_INVALID_CONSTANT: close any publisher issue not in current set
            const liveHPEPubs = new Set([...byPubHPE.keys()]);
            for (const iss of openHPE) {
              const m = /^URL ERROR \(HPE_INVALID_CONSTANT\): (.+)$/.exec(iss.title || '');
              const pub = m && m[1] ? m[1].trim() : null;
              if (!pub || !liveHPEPubs.has(pub)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no outstanding HPE_INVALID_CONSTANT for this publisher. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // ERR_FR_TOO_MANY_REDIRECTS: close any publisher issue not in current set
            const liveREDIRPubs = new Set([...byPubREDIR.keys()]);
            for (const iss of openREDIR) {
              const m = /^URL ERROR \(ERR_FR_TOO_MANY_REDIRECTS\): (.+)$/.exec(iss.title || '');
              const pub = m && m[1] ? m[1].trim() : null;
              if (!pub || !liveREDIRPubs.has(pub)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no outstanding ERR_FR_TOO_MANY_REDIRECTS for this publisher. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // SMPTE.resolvedHref.standards-prefix: close any publisher issue not in current set
            const liveSMPTEStdPubs = new Set([...byPubSMPTEStd.keys()]);
            for (const iss of openSMPTEStd) {
              const m = /^EXPECTATION MISMATCH \(SMPTE\.resolvedHref\.standards-prefix\): (.+)$/.exec(iss.title || '');
              const pub = m && m[1] ? m[1].trim() : null;
              if (!pub || !liveSMPTEStdPubs.has(pub)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no outstanding SMPTE.resolvedHref.standards-prefix mismatches for this publisher. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // 400s: close any docId issue not in current set
            for (const iss of open400) {
              const m = /^URL ERROR \(400\): (.+)$/.exec(iss.title || '');
              const docId = m && m[1] ? m[1].trim() : null;
              if (!docId || !current400.has(docId)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no longer present as 400. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // ERR_BAD_REQUEST: close any docId issue not in current set
            const currentBR = new Set(listBR.map(i => i.docId));
            for (const iss of openBR) {
              const m = /^URL ERROR \(ERR_BAD_REQUEST\): (.+)$/.exec(iss.title || '');
              const docId = m && m[1] ? m[1].trim() : null;
              if (!docId || !currentBR.has(docId)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no longer present as ERR_BAD_REQUEST. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            // UNABLE_TO_VERIFY_LEAF_SIGNATURE: close any docId issue not in current set
            const currentLEAF = new Set(listLEAF.map(i => i.docId));
            for (const iss of openLEAF) {
              const m = /^URL ERROR \(UNABLE_TO_VERIFY_LEAF_SIGNATURE\): (.+)$/.exec(iss.title || '');
              const docId = m && m[1] ? m[1].trim() : null;
              if (!docId || !currentLEAF.has(docId)) {
                await github.rest.issues.createComment({
                  owner, repo, issue_number: iss.number,
                  body: `Resolved: no longer present as UNABLE_TO_VERIFY_LEAF_SIGNATURE. ${runTag}. Closing.`
                });
                await github.rest.issues.update({ owner, repo, issue_number: iss.number, state: 'closed' });
              }
            }

            core.info('Issue upsert/close pass complete.');
