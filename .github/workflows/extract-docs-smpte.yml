name: Extract Documents - SMPTE

on:
  schedule:
    - cron: "15 04 * * 1"   # 21:15 PT, Mon (cron in UTC)
  workflow_dispatch: 

permissions:
  contents: write
  pull-requests: write

jobs:
  extract:
    name: Extract Docs
    runs-on: ubuntu-latest
    env:
      KEYWORD_VALIDATION_MODE: warn

    steps:
      - name: Mint PrZ3 Unit token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          token: ${{ steps.app-token.outputs.token }}

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install axios cheerio dayjs

      - name: Run extraction
        env:
          IS_PR_RUN: true
          PR_LOG_PATH: "${{ runner.temp }}/pr-log.log"
          #Override script defaults if needed
          #MSR_CONSOLE_BUDGET: "3670016"       # ~3.5 MiB
          #MSR_HEARTBEAT_EVERY: "50"          # print heartbeat every XX docs
          #MSR_HEARTBEAT_PREFIX: " 💓 ... still processing"
        run: node src/main/scripts/extractDocs.js --provider smpte

      - name: Upload full extract log (post-extraction)
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: extract-full-log
          path: ${{ runner.temp }}/extract-full.log

      - name: Check if PR should be skipped
        id: skipcheck
        run: |
          LOG="${{ runner.temp }}/pr-log.log"
          if [ ! -f "$LOG" ]; then
            echo "No PR log found at $LOG; defaulting to skip."
            echo "skip=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          if grep -q "No new or updated documents" "$LOG"; then
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Validate document registry
        if: steps.skipcheck.outputs.skip == 'false'
        run: npm run validate -- --warn 2>&1 | tee "${{ runner.temp }}/validate.log"

      - name: Append keyword warnings to PR log
        if: steps.skipcheck.outputs.skip == 'false'
        run: |
          VLOG="${{ runner.temp }}/validate.log"
          PRLOG="${{ runner.temp }}/pr-log.log"
          [ -f "$VLOG" ] || exit 0
          [ -f "$PRLOG" ] || touch "$PRLOG"

          KW_BLOCK="$(awk '
            /Unknown keywords not in src\/main\/config\/site.json#controlledKeywords:/ { in_block=1; print; next }
            in_block {
              print
              if ($0 ~ /registry has keywords outside controlledKeywords in src\/main\/config\/site.json/) {
                in_block=0
                exit
              }
            }
          ' "$VLOG")"

          if [ -n "$KW_BLOCK" ]; then
            {
              echo
              echo "### ⚠️ Unknown Keywords (will fail strict \`npm run validate\`)"
              echo
              echo '```text'
              printf '%s\n' "$KW_BLOCK"
              echo '```'
              echo
            } >> "$PRLOG"
          fi

      - name: Canonicalize document registry
        if: steps.skipcheck.outputs.skip == 'false'
        env:
          IS_PR_RUN: true
          PR_LOG_PATH: ${{ runner.temp }}/pr-log.log
        run: npm run canonicalize

      - name: Commit canonicalized changes (if any)
        if: steps.skipcheck.outputs.skip == 'false'
        run: |
          git config user.name "PrZ3 Unit"
          git config user.email "prz3-unit[bot]@users.noreply.github.com"
          git add -A src/main/data/docs/
          git diff --cached --quiet || git commit -m "Canonicalize document registry"

      - name: Commit PR update details file
        run: |
          git config user.name "PrZ3 Unit"
          git config user.email "prz3-unit[bot]@users.noreply.github.com"
          git add src/main/logs/extract-runs/pr-log-full-*.log
          git diff --cached --quiet || git commit -m "Add PR update details file"

      - name: Resolve details file & blob SHA
        id: details
        run: |
          FILE="$(git ls-files 'src/main/logs/extract-runs/pr-log-full-*.log' | tail -n1)"
          if [ -z "$FILE" ]; then
            echo "file=" >> $GITHUB_OUTPUT
            echo "blob=" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "file=$FILE" >> $GITHUB_OUTPUT
          BLOB_SHA="$(git rev-parse "HEAD:$FILE")"
          echo "blob=$BLOB_SHA" >> $GITHUB_OUTPUT

      - name: Read PR log
        if: steps.skipcheck.outputs.skip == 'false'
        id: prlog
        run: |
          LOG="${{ runner.temp }}/pr-log.log"
          DELIM="PR_BODY_$(date +%s%N)"
          if [ -f "$LOG" ]; then
            {
              printf 'body<<%s\n' "$DELIM"
              cat "$LOG"
              printf '\n%s\n' "$DELIM"
            } >> "$GITHUB_OUTPUT"
          else
            echo "PR log not found at $LOG; using fallback body."
            {
              printf 'body<<%s\n' "$DELIM"
              echo 'Automated extract: changes detected (details log not available).'
              printf '%s\n' "$DELIM"
            } >> "$GITHUB_OUTPUT"
          fi

      - name: Set timestamp env var
        if: steps.skipcheck.outputs.skip == 'false'
        id: timestamp
        run: echo "ts=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_ENV

      - name: Commit and create PR
        if: steps.skipcheck.outputs.skip == 'false'
        id: cpr
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ steps.app-token.outputs.token }}
          commit-message: "chore: Update document registry from SMPTE sources"
          commit-options: '--allow-empty'
          branch: chore/extract-docs-smpte     
          labels: automated, msr, extract, smpte  
          delete-branch: true      
          title: "Update document registry - extract SMPTE (${{ env.ts }})"
          body: ${{ steps.prlog.outputs.body }}
          add-paths: |
            src/main/data/docs
            src/main/reports/masterReferenceIndex.json
          base: main

      # NEW: rewrite the PR body to use the PR Files diff anchor (not the raw URL)
      - name: Update PR body with diff link
        if: steps.skipcheck.outputs.skip == 'false' && steps.details.outputs.file != ''
        uses: actions/github-script@v9
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const { owner, repo } = context.repo;
            const prNumber = Number('${{ steps.cpr.outputs.pull-request-number }}');
            if (!prNumber) { core.info('No PR number; skipping.'); return; }
            const token = '__PR_DETAILS_DIFF_LINK__';
            const blob = '${{ steps.details.outputs.blob }}';
            const diffUrl = `https://github.com/${process.env.GITHUB_REPOSITORY}/pull/${prNumber}/files#diff-${blob}`;
            const { data: pr } = await github.rest.pulls.get({ owner, repo, pull_number: prNumber });
            const oldBody = pr.body || '';
            const newBody = oldBody.split(token).join(diffUrl);
            if (newBody !== oldBody) {
              await github.rest.pulls.update({ owner, repo, pull_number: prNumber, body: newBody });
              core.info('PR body updated to use diff anchor link (token replace).');
            } else {
              core.info('Token not found; nothing to replace.');
            }
